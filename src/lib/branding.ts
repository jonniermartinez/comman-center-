/**
 * Nombres de marca, en un solo lugar para cambiarlos sin buscar por el código.
 *
 * `APP_NAME` es el producto: es lo que va en el logo del sidebar y en el título
 * de la pestaña. `OPERATOR_NAME` es la empresa que usa el sistema y cuyos
 * clientes son las "empresas" de la app.
 */
export const APP_NAME = "Command Center"

export const OPERATOR_NAME = "TrámitesBuga"
